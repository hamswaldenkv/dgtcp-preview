/* eslint-disable @typescript-eslint/no-explicit-any */
export const generatePacksFromRequest = async (id: string) => {};

export const generatePack = async ({
  reference,
  inventoryRequestId,
}: {
  inventoryRequestId: string;
  reference: string;
}) => {};
